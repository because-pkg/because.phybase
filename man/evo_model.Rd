% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structure_phylo.R
\name{evo_model}
\alias{evo_model}
\title{Attach Evolutionary Model Specifications to Phylogenetic Structures}
\usage{
evo_model(tree, model = "BM", n_alpha = 50)
}
\arguments{
\item{tree}{A \code{phylo} or \code{multiPhylo} object.}

\item{model}{A named character vector mapping response variables to evolutionary models.
Valid options are \code{"BM"} (Brownian Motion) and \code{"OU"} (Ornstein-Uhlenbeck).
If unnamed and length 1, the model is applied globally to all phylogenetically
structured responses in the SEM.}

\item{n_alpha}{Number of points for the OU \code{alpha} parameter grid (default 50).}
}
\value{
The original tree object with an \code{"evo_model"} attribute attached.
}
\description{
This helper attaches an \code{"evo_model"} attribute to a \code{phylo} or \code{multiPhylo} object.
By default, \code{because} assumes a Brownian Motion (BM) model. You can use this
function to specify node-specific evolutionary models (e.g., Ornstein-Uhlenbeck)
for different response variables within the same causal graph.
}
\examples{
\dontrun{
# Apply an OU model globally with default grid (50 points)
my_tree <- evo_model(my_tree, "OU")

# Increase grid resolution for better precision
my_tree <- evo_model(my_tree, "OU", n_alpha = 100)

# Apply BM to TraitA and OU to TraitB
my_tree <- evo_model(my_tree, c(TraitA = "BM", TraitB = "OU"))
}
}
